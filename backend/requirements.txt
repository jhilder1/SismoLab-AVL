# SismoLab AVL — Backend Dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Utilities
python-multipart>=0.0.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# CORS support (for React frontend)
# (included in fastapi via starlette)
